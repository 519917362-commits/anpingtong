# 安平同城网 - 项目总结

## 目标
仿 cnboling.cn 风格的安平县本地生活分类信息网站。

## 技术栈
- **后端**: Node.js + Express + better-sqlite3（原生 SQLite，无外部 DB 服务）
- **前端（用户端）**: React 18 + Vite + TailwindCSS
- **前端（管理后台）**: React 18 + Vite + TailwindCSS
- **密码加密**: bcryptjs

## 项目结构
```
anping-info/
├── server/          # Express API 服务端
│   ├── index.js     # 入口文件，同时托管前端静态文件
│   ├── db/
│   │   ├── schema.js   # 建表 + 种子数据（含用户/分类/帖子）
│   │   └── anping.db   # SQLite 数据库文件
│   ├── routes/
│   │   ├── auth.js     # 注册/登录/登出
│   │   ├── posts.js    # 帖子浏览/搜索/分类
│   │   ├── user.js     # 用户帖子管理
│   │   └── admin.js    # 管理后台 API
│   ├── middleware/
│   └── controllers/
├── client/          # 用户端前台（已构建）
│   ├── src/
│   │   ├── pages/  # Home, Category, PostDetail, Login, Register, PostCreate, MyPosts, Search
│   │   ├── components/Layout.jsx
│   │   └── context/AuthContext.jsx
│   └── dist/       # 构建产物
├── admin/           # 管理后台（已构建）
│   ├── src/
│   │   └── pages/
│   └── dist/       # 构建产物
└── README.md
```

## 启动方式
```bash
cd anping-info/server
npm install
node index.js
# 服务端: http://localhost:3001
# 前台:   http://localhost:3001
# 后台:   http://localhost:3001/admin
```

## 数据库账号
| 角色 | 用户名 | 密码 |
|------|--------|------|
| 管理员（后台） | admin | admin123 |
| 用户前台 | test001 | 123456 |
| 用户前台 | test002 | 123456 |

## 数据库现状
- 分类: 8 个（房屋租售/车辆服务/招聘求职/商务服务/二手物品/生活服务/教育培训/其他信息）
- 用户: 4 个（含 admin）
- 帖子: 9 条（已审核 approved）

## 问题修复记录
1. seed.js 循环依赖：schema.js 导入 seed.js，seed.js 又导入 schema.js（ESM 初始化顺序问题）。**解决**：将 seed 逻辑合并到 schema.js 中。
2. 用户表未创建：admin 账号在 admins 表，不在 users 表，导致帖子关联用户 ID 时找不到。**解决**：schema.js 中增加种子用户插入到 users 表。
3. admin/admin123 登录 API 需 BCrypt 验证：原 auth.js bcrypt.compare 逻辑已就位。

## API 端点
- `GET  /api/health` - 健康检查
- `POST /api/auth/register` - 注册
- `POST /api/auth/login` - 登录
- `GET  /api/posts?page=&pageSize=&category=&keyword=` - 帖子列表
- `GET  /api/posts/categories` - 分类列表
- `GET  /api/posts/:id` - 帖子详情
- `POST /api/user/posts` - 发布帖子（需登录）
- `GET  /api/admin/posts` - 管理端帖子列表（需 admin 权限）
- `PATCH /api/admin/posts/:id/status` - 修改帖子状态
